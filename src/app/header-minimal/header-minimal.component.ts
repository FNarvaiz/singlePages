import { Component } from '@angular/core';
// import Parallax from 'parallax-js'; // Import the 'Parallax' class from the 'parallax-js' library

@Component({
  selector: 'app-header-minimal',
  standalone: true,
  imports: [],
  templateUrl: './header-minimal.component.html',
  styleUrl: './header-minimal.component.css'
})
export class HeaderMinimalComponent {
  // scene = document.getElementById('scene');
  // parallaxInstance = new Parallax(scene);
}
